# SPEC-{{NNN}}: {{название}}

Статус: draft <!-- draft | approved | in-progress | done -->

## Зачем

<!-- Проблема или ценность. 2–5 предложений, понятных без контекста чата. -->

## Что делаем

<!-- Наблюдаемое поведение системы после реализации. Не "как", а "что". -->

## Границы

<!-- Что сознательно НЕ делаем в этой фиче. Это защита от расползания скоупа. -->

## Критерии приёмки

<!-- Проверяемые утверждения. Каждый критерий должен превращаться в тест.
     Критерии нумеруются последовательно внутри спеки: AC-{{NNN}}-1,
     AC-{{NNN}}-2, … — {{NNN}} тот же номер спеки, что и в названии выше.
     Токен целиком входит в формулировку и дальше используется как тег
     теста, который критерий закрывает (конвенция тегирования — skills/tdd).
     Номер спеки в токене — то, что не даёт одинаковому порядковому номеру
     критерия в разных спеках случайно засчитать чужое покрытие (issue
     #171, docs/adr/010-spec-scoped-ac-tokens.md): гейт сопоставляет тег
     теста и критерий по литеральному совпадению всей строки токена, а не
     по имени файла спеки. Пока тест критерия только запланирован, /plan
     помечает критерий аннотацией «(ждёт #N)» с номером issue — сразу
     после токена, на той же строке: «AC-{{NNN}}-1 (ждёт #41): …» (в
     другом месте или на переносе строки аннотация не действует) —
     ac-check принимает такой AC без теста; PR, добавляющий тег-тест,
     снимает аннотацию. -->

- [ ] AC-{{NNN}}-1: ...
- [ ] AC-{{NNN}}-2: ...

## Переходные состояния

<!-- Обязательный вопрос, если спека вводит или меняет гейт/инвариант:
     что гейт показывает МЕЖДУ апрувом спеки и полной реализацией?
     что — при включении на уже существующем состоянии (репозиторий с
     историей, открытые PR)? «Переходных состояний нет» — тоже ответ,
     но написанный явно. Спека без гейтов — удали секцию. -->

## Открытые вопросы

<!-- Что требует решения человека. Пусто = вопросов нет. -->
