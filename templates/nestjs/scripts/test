#!/usr/bin/env bash
# Контракт agent-dev-kit: все тесты проекта (NestJS по умолчанию использует jest).
# --passWithNoTests: свежий проект без тестов не должен блокировать Stop-гейт.
set -eo pipefail
cd "$(dirname "$0")/.."

npx jest --passWithNoTests
