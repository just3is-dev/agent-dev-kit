# Контракт команд проекта

Каждый проект, работающий с agent-dev-kit, обязан иметь три исполняемых скрипта.
Хуки плагина зовут их вслепую, не зная стека проекта. Реализация скриптов —
дело проекта (генерируется при инициализации из шаблона под выбранный стек).

| Скрипт | Аргументы | Что делает | Требования |
|---|---|---|---|
| `scripts/check` | `[файлы...]` | Статические проверки: типы + линт | Без аргументов — весь проект, плюс AC-трассируемость (см. ниже). С аргументами — только затронутая область, быстро (цель < 30 сек), без AC-трассируемости. Exit ≠ 0 при любой ошибке, ошибки — в stdout/stderr. |
| `scripts/test` | — | Все тесты проекта | Exit ≠ 0 при провале. Вывод должен показывать, какие тесты упали. Для сервисов сюда же входит smoke «приложение стартует» (bootstrap на фейках с немедленным выходом): тест-раннеры транспилируют код сами и не ловят ошибки рантайм-сборки — резолв модулей, ESM/CJS, DI. |
| `scripts/fix` | `[файлы...]` | Автоформат и авто-исправимый линт | Молча правит файлы. Exit ≠ 0 только если исправить невозможно. |

## Правила

- Скрипты должны работать из корня проекта без предварительной настройки
  окружения (сами активируют venv, зовут npx и т.п.).
- В монорепе скрипт сам определяет по путям файлов, какие пакеты затронуты,
  и запускает проверки только для них. Готовый корневой диспетчер —
  `templates/monorepo/`: пакетом считается любая поддиректория со своим
  `scripts/check`, новые пакеты подхватываются автоматически.
- Добавил в проект новый язык/сервис — обязан расширить реализацию скриптов,
  чтобы контракт покрывал и его. Код вне контракта = непроверяемый код.
- Полный прогон `scripts/check` (без аргументов) вызывает `scripts/ac-check .`
  хвостом, если этот хелпер исполняем: он падает, если у какого-то AC
  approved-спеки нет ни одного помеченного теста. Исключение — переходное
  состояние милестоуна: критерий с аннотацией `(ждёт #N)` сразу после
  токена («AC-7 (ждёт #52)»: тест запланирован в issue #N; ставит `/plan`,
  снимает PR, добавляющий тег-тест) принимается без теста. Полнота без
  скидок проверяется на границе милестоуна: `ac-check --complete`
  (вызывает `/consolidate`) требует тест для каждого AC, игнорируя
  аннотации. Хелпер копируется
  project-init'ом из плагина (`hooks/scripts/ac-check.sh` →
  `scripts/ac-check`); отсутствие хелпера ничего не ломает. Кастомный
  стек (шаг 7 `/project-init`) обязан вызывать его так же.
  Обновление копии: инициализация копирует хелпер один раз, обновления
  плагина в уже инициализированные проекты сами не долетают.
  `/project-init` для повторного запуска не годится: шаг 5 команды
  безусловно кладёт `templates/<стек>/scripts/*` и `ci.yml` без охраны
  от перезаписи — это не та же ветка, что «существующий репозиторий» шага
  9, там охрана касается только серверных настроек и issue-шаблонов, а не
  файлов шага 5. Повторный прогон заодно с хелпером затрёт
  кастомизированный `scripts/test` и CI проекта. Путь обновления —
  прямое копирование одного файла из установленного плагина: подстановка
  `${CLAUDE_PLUGIN_ROOT}` работает только внутри hooks/markdown-контекста
  Claude Code, в обычном shell эта переменная не задана, путь нужно найти
  самостоятельно —
  `AC_CHECK_SRC=$(find ~/.claude -path
  '*agent-dev-kit*/hooks/scripts/ac-check.sh' 2>/dev/null | head -1); if [
  -z "$AC_CHECK_SRC" ]; then echo "ac-check.sh не найден под ~/.claude —
  agent-dev-kit не установлен по ожидаемому пути" >&2; else cp
  "$AC_CHECK_SRC" scripts/ac-check && chmod +x scripts/ac-check; fi`
  в корне проекта; остальные файлы проекта эта команда не касается.
  Пустой результат `find` (плагин не установлен туда, куда ожидалось) без
  проверки `-z "$AC_CHECK_SRC"` раньше падал на голом `cp "" scripts/ac-check`
  невнятным `cp: : No such file or directory`; проверка выше печатает
  вместо этого понятную причину (issue #122).
  Признак устаревшей копии — точное сравнение с найденным исходником:
  `diff scripts/ac-check "$AC_CHECK_SRC"` — непустой вывод означает, что
  копия отстала. Прежний узкий признак — она не поддерживает
  аннотацию `(ждёт #N)` (issue #54): `grep -q 'ждёт #' scripts/ac-check`
  ничего не находит — ловит только эту одну аннотацию и слеп к остальным
  правкам хелпера, не связанным с ней (issue #122); `diff` ловит любое
  расхождение файлов, не только эту аннотацию. Прогон на проекте со
  старой копией: approved-спека с критерием `AC-7 (ждёт #52)` без теста —
  на проекте со старой копией `scripts/check` красный (копия трактует
  критерий как непокрытый); после команды копирования выше та же спека
  даёт зелёный `scripts/check` (аннотированный критерий больше не требует
  теста).
- Скрипты — единственная точка входа: CI зовёт те же `scripts/check` и
  `scripts/test`, что и хуки. Локальные гейты и CI не могут разойтись.

## Версионирование плагина

Кит распространяется как плагин Claude Code: потребитель через
GitHub-маркетплейс получает не HEAD репозитория, а снапшот в локальном
кэше, версионированный по `version` в `.claude-plugin/plugin.json`. Это
единственный сигнал потребителю, что кэш устарел, — бамп версии
обязателен, а не по желанию автора PR.

**Файлы плагина**: `commands/`, `agents/`, `skills/`, `hooks/`, `templates/`, `docs/contract.md`, `docs/config.md`, `docs/adr/`, `.claude-plugin/plugin.json`. Критерий не «это документация», а «читается исполняемой частью плагина (командой, хуком, агентом, скиллом) из кэша в рантайме через `${CLAUDE_PLUGIN_ROOT}`» (грепом по
`${CLAUDE_PLUGIN_ROOT}.*docs`, issue #166): `docs/contract.md` и
`docs/config.md` — `project-init.md` читает их из кэша плагина через
`${CLAUDE_PLUGIN_ROOT}`; `docs/adr/` целиком — `stats.md` читает
`docs/adr/001-journal-event-schema.md` через `${CLAUDE_PLUGIN_ROOT}`, а
директория не файл: следующая команда может начать читать любой другой
ADR по номеру.

Три уровня бампа:

- **patch** — любой PR, меняющий файлы плагина.
- **minor** — закрытие вехи (SPEC-NNN или CONS-NNN): бамп делает
  `/consolidate` тем же шагом, что закрывает milestone.
- **major** — ломающее изменение контракта (`scripts/check|test|fix`,
  схема журнала, формат `adk.config.json`); до 1.0.0 не используется —
  breaking помечается в релизе.

Не требуют бампа — не читаются исполняемыми частями плагина (командами,
хуками, агентами, скиллами) через `${CLAUDE_PLUGIN_ROOT}` в рантайме:
`docs/specs`, `docs/plans`, `docs/observability`, `tests/`, `README`,
`.github/`, `scripts/`, `.claude-plugin/marketplace.json`.

Одноимённые пути `docs/specs`, `docs/plans` встречаются и в
`commands/project-init.md`, `commands/plan.md`, `commands/spec.md`,
`hooks/scripts/ac-check.sh` — но это пути в репозитории ПРОЕКТА,
использующего кит (там лежат спека и план самого этого проекта), а не
в кэше плагина; к списку выше это не относится.

`.claude-plugin/marketplace.json` — метаданные листинга плагина в
маркетплейсе (имя, описание, путь к источнику); не читается командами
или хуками плагина и не является инструкцией их поведения.

Сравнение с base-веткой PR делает CI-гейт, а не локальный `scripts/check`:
такое сравнение требует сети/истории git, а не только рабочей копии —
это работа CI, не локальных гейтов.

Политика действует с момента её фиксации здесь; принудительно проверяет
её CI-гейт из отдельной задачи (issue #151) — до его появления patch не
бампается вручную на каждом PR постфактум. Первый бамп (0.1.0 → 0.2.0)
— отдельная разовая задача (issue #156), не побочный эффект правки этого
раздела.

## Как контракт используют хуки плагина

- `PostToolUse` (после каждого Edit/Write): `scripts/check <файл>`.
  Ошибки возвращаются агенту, он обязан их исправить.
- `Stop` (агент завершает работу): `scripts/test`, если в рабочем дереве
  есть изменения. Провал теста не даёт завершить работу.

Если скриптов в проекте нет, хуки молча пропускают проверки — плагин
безопасно установлен глобально и не мешает посторонним репозиториям.
