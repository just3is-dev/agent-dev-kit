# Выжимка журнала прогонов: веха consolidation

Дата границы: 2026-08-16. Источник — агрегаты `adk-stats.sh` по `.adk/logs/`
(сам журнал в git не попадает, ADR-001); фиксация на границе вехи —
процесс `/consolidate`.

## Агрегаты

Накопленным итогом по всему журналу (веха consolidation — задачи #72–#76):

- Всего задач: 20
- Средние круги ревью: 1.8
- Доля застреваний: 0% (0/20)
- Разрез по типам (количество / средние круги ревью):
  - task: 14 / 2.1
  - consolidate: 5 / 1.0
  - fastFollow: 1 / 2.0
- Динамика по неделям: 2026-W33 — 20 задач, 1.8 круга

Вклад вехи: 5 задач типа `consolidate`, каждая принята с первого круга
ревью, застреваний нет. Для сравнения — веха SPEC-002: 15 задач по 2.1
круга (`docs/observability/SPEC-002.md`). Задачи консолидации дешевле в
ревью, чем плановые: дифф без изменения семантики проверяется быстрее.

Прогон вехи прошёл одним запуском `/autopilot` (лимит 5, все пять задач
смержены). Единственное отклонение — не процессное: первый `gh pr merge`
заблокировал классификатор разрешений сессии, а не хук; после явного
разрешения merge выполнялся штатно.

Оговорка достоверности прежняя: круги, проведённые через `/review`, в
журнал не записываются (issue #79) — среднее по самым проблемным задачам
занижено. На этой вехе `/review` не запускался ни разу, поэтому её
собственные 1.0 круга оговоркой не искажены.

## Петля сбежавших дефектов

Распределение «Сбежал от: <гейт>» по баг-issues (label `type:bug`):

- reviewer — 3 (#21, #24 — несостыковки контракта писатель/читатель;
  #82 — гейт не знает конвенций нового стека)
- процесс — 1 (#54)
- check, test — 0

Расхождения между выборкой по label и поиском `«Сбежал от»` нет: поиск
дополнительно возвращает issues самих спек, где поле определяется, и
задачи других типов — перевешивать label не на что.

Reviewer остаётся «текущим» гейтом вторую веху подряд, и разрыв вырос
(2 → 3). Усиление — issue #81: к пункту чек-листа про стороны контракта
добавлен пункт про видимость нового стека гейтам проекта (`ac-check`,
`scripts/check`, `scripts/test`), закрывающий грань попадания #82.

## Полнота критериев приёмки

`ac-check.sh --complete .` на границе вехи проходит: каждый AC
утверждённых спек покрыт тестом, аннотаций «ждёт #N» без теста не
осталось.
